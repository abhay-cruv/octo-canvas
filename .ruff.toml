line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "RUF"]
ignore = ["B008"]  # FastAPI / Typer use `param = Depends(...)` as the standard pattern.

[format]
quote-style = "double"
indent-style = "space"
